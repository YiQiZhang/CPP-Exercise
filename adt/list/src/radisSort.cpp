// radisSort.cpp

#include <vector>
#include <list>

using std::vector;
using std::list;

template<typename T>
vector<T> radisSort(const vector<T>::iterator &beg, const vector<T>::iterator end)
{
	vector<list<T>> radis;
	// 

	vector<T> res;

	return res;
}

int main()
{
	vector<int> mylist = {23};

	return 0;
}